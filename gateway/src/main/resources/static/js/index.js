function submitSingIn() {
      // Здесь может быть ваша логика перед перенаправлением
      window.location.href = "/login";
    }

function submitSingUp() {
      // Здесь может быть ваша логика перед перенаправлением
      window.location.href = "/create";
    }